#include <iostream>
using namespace std;

int main(){
    int point, pointsAmount, even, odd;
    cin >> pointsAmount;
    vector<int> points(partsAmount);
    for (int i = 0; i < pointsAmount; i++) {
        cin >> points[i];
    }
    for (i=0, i<pointsAmount, i ++){
        if (i % 2 == 0){
            even += poins[i]
        } else{
            odd += points[i]
        }
    if (even > odd){
        cout << "VOu ajudar"
    }
    }
}